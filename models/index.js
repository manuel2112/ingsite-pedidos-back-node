const { Usuario } = require('./Usuario');
const { Parametro } = require('./Parameter');
const { Articulo } = require('./Article');
const { Familia } = require('./Family');
const { Cliente } = require('./Client');
const { Preventa } = require('./Preventa');
const { Detpreventa } = require('./Detpreventa');

module.export = {
    Usuario,
    Parametro,
    Articulo,
    Familia,
    Cliente,
    Preventa,
    Detpreventa
}